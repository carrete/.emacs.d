;; -*- coding: utf-8; mode: emacs-lisp -*-

;; (use-package evil
;;   :init
;;   (setq evil-toggle-key "C-`")
;;   :config
;;   (evil-mode t)
;;   (evil-define-command evil-quit (&optional force)
;;                        :repeat nil
;;                        (interactive "<!>")
;;                        (kill-buffer)))

(provide 'setup-evil)
